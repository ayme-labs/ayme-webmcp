{
  "name": "ayme-webmcp",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.24.0",
  "scripts": {
    "build": "turbo run build",
    "boundaries": "turbo boundaries",
    "check": "pnpm run build && pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run test:e2e && pnpm run boundaries",
    "prepare": "lefthook install",
    "hooks:install": "lefthook install",
    "commitlint": "commitlint --verbose",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@commitlint/cli": "^21.0.2",
    "@commitlint/types": "^21.0.1",
    "@types/node": "^24.0.0",
    "eslint": "^10.9.1",
    "lefthook": "^2.1.8",
    "turbo": "^2.10.12",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.68.0"
  }
}
